services:
  - type: web
    name: bydpower-api
    runtime: python
    buildCommand: bash build.sh
    startCommand: gunicorn app:app --timeout 120
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0